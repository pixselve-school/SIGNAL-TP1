A = imread('neuro.jpeg');

oneChannel = rgb2gray(A);

real = double(oneChannel);
colormap(gray);

imgGaussien = filtre_gaussien(real, 8, 6);
img


laplacienne = [0,1,0; 1,-4,1; 0,1,0];
laplacimg = conv2(imgGaussien, laplacienne, 'same');


imgOut = real + 0.8*laplacimg;
imagesc(imgOut);

